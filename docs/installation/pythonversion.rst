Python version
==============

The tool was created to be able to run on Python version **3.5+**. It is also possible that the project will run on
previous versions of Python 3, however, I really recommend you to update your Python to the newest version.